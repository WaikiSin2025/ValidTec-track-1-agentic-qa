# GitHub Setup

Suggested repository name: `validtec-track1-agentic-qa`

Suggested description:

> Controlled agentic QA workflow: requirements → risk-based scenarios → human approval → automation handoff → evidence.

## Command-line upload

```bash
git init
git add .
git commit -m "Initial Track 1 agentic QA portfolio project"
git branch -M main
git remote add origin https://github.com/<YOUR-GITHUB-USERNAME>/validtec-track1-agentic-qa.git
git push -u origin main
```

Before making the repository public, verify that only synthetic data is present.
