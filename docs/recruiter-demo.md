# Five-Minute Recruiter Demo

1. Open `examples/feature_ticket.json` and show a realistic feature requirement.
2. Run the workflow **without** `--approve` and show that automation generation is blocked.
3. Review `02_test_scenarios.json` and explain traceability/risk ranking.
4. Re-run with `--approve`.
5. Open `04_automation_spec.json` and explain how Track 5 consumes the deterministic handoff.

## Talking point

"I use AI-style agent boundaries to accelerate analysis and test design, but I keep explicit QA approval and deterministic automation at the center of the workflow."
